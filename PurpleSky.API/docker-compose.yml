services:
  postgres:
    image: postgres:16
    container_name: ttrpgpsky-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 05989
      POSTGRES_DB: purplesky
    ports:
      - "5432:5432" 
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
